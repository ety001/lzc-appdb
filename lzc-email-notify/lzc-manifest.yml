application:
  subdomain: email-notify
  # 本应用需要持续在后台轮询邮箱，声明后台任务避免平台主动休眠
  background_task: true
  routes:
    - /=http://server:8000

services:
  server:
    image: registry.lazycat.cloud/ety001/ety001/lzc-email-notify:f35acfebfd0b81f6
    binds:
      - /lzcapp/var/data:/data
    environment:
      - TZ=Asia/Shanghai
